# NVIDIA Qwen3.6-27B NVFP4 qualification — 2026-07-03

## Exact serving configuration

- Checkpoint: `nvidia/Qwen3.6-27B-NVFP4`
- Revision: `0893e1606ff3d5f97a441f405d5fc541a6bdf404`
- Weight quantization: NVIDIA ModelOpt mixed FP8/NVFP4
- KV cache: FP8 E4M3
- Decoding: autoregressive
- Runtime image:
  `local/sglang-runtime:v0.5.14-cu130-runtime-distro1.9.0-nvfp4fix`
- Image ID:
  `sha256:9b8f86ccafb1b7e8856e8e689088f9d868febef965028b1d02a18a3083d67a5c`
- Context length: 262,144
- `max_running_requests`: 3
- `max_queued_requests`: 3
- Qualified pool: 827,041 tokens
- Deployed co-resident pool ceiling: 524,288 tokens
- `mem_fraction_static`: 0.60

## Launch and pool

- Clean cold load reached HTTP 200.
- Weight load: 22.04 GiB in approximately 112–140 seconds across repeated runs.
- Final measured KV pool: 827,041 tokens.
- KV tensors: FP8 E4M3.
- Host `MemAvailable` after load: 40 GiB.

## Agentic capability evidence

- Chat completion: exact coherent response `PACKAGED READY`.
- Tool calling: `lookup({"item": "alpha"})` with `finish_reason=tool_calls`.
- Grammar-constrained structured output: strict schema returned
  `{"ready": true}`.

## Three-request capacity gate

Three chat-completion requests ran concurrently:

- Prompt tokens per request: 250,020
- Aggregate prompt tokens: 750,060
- Completion: `CAPACITY OK` for all three
- Per-request elapsed: 405.269–405.937 seconds
- Aggregate elapsed: 405.946 seconds
- No admission rejection, OOM, restart, or incoherent output.

The realized 827,041-token pool exceeds the three-request aggregate prompt by
76,981 tokens.

The 827,041-token result is the capacity qualification, not the deployed
co-resident policy. The production profile was subsequently capped at 524,288
tokens so an operator-configured helper could hold a separate token pool.

## Cancellation and recovery

- A 100K-token request was abandoned by the client.
- A subsequent short request returned `RECOVERED` in 5.57 seconds.
- Health remained HTTP 200.
- Container restart count remained zero.

## Known limitations

- FP8 KV scaling factors are not supplied by the checkpoint; SGLang defaults
  them to 1.0 and warns that accuracy may be affected. Role-level behavior
  passed, but broader quality evaluation remains appropriate.
- EAGLE/MTP speculative decoding was not enabled or qualified.
- Multimodal behavior was not part of the `agentic` role gate.

## Managed co-residency verification

With Qwen capped at 524,288 tokens and an operator-configured helper using a
separate 393,216-token pool:

- both managed endpoints reached HTTP 200;
- Qwen used three running-request slots and the helper used nine;
- both passed visible chat, parsed tool-call, and strict JSON-schema probes;
- both survived a cold host reboot with zero service restarts;
- the readiness gate delayed the helper until Qwen returned HTTP 200;
- post-load host `MemAvailable` was 17.51 GiB.
