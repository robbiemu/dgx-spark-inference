# Architecture

The canonical architecture document is [`docs/architecture.md`](docs/architecture.md).

This redirect exists so historical links to a root-level `architecture.md` keep
working. The authoritative prose — the binding system, the launch flow, weight
resolution, capability enforcement, and the v1→v2 evolution — lives in
`docs/architecture.md` to avoid two copies drifting.
